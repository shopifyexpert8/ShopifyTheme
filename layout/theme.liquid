<html>
<head>
        {{ content_for_header }}
        {{ 'style.css' | asset_url | stylesheet_tag }}
</head>
<body>
        {% render 'header' %}
        {{ content_for_layout }}
        <script src="{{ 'scripts.js' | asset_url }}" defer="defer"></script>
</body>
</html>