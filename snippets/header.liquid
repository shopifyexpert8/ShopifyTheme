<header>
    <h1>{{ shop.name }}</h1>
    <ul>
        <!-- Main menu -->
    {% for link in linklists.main-menu.links -%}
    {{ link.title | link_to: link.url }}
    {%- endfor %}
    </ul>
    <img src="{{ 'standard.jpg' | asset_url }}"/>

</header>